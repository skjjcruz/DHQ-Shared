# Integrated engine, assessment and Scout checkpoint — September20

Actual canonical union source7b483307b9f988cf287b73673a1f515840d18192 combines the prior reviewed session/ESPN/Yahoo/capital source with Scout legacy-state dc1e501, explicit engine1e7ef86+6d3d604, assessment e318219 and named background-loader capability fec9f46. No actual-owner main, release pin, backend or database changes.

Independent native reviewer reproduced4engine defects and cleared their exact corrections (review aebabe7). Root separately reviewed Scout producer/consumer source and independently ran5state+18transaction+14protocol+14context+21validation,11actualcaller and3Chrome layouts (consumer review075bacf). Root inspected the53-line assessment and6-line named-loader deltas and reran7assessment groups including full healthy-output equality; no material finding. The explicit capability prevents an older shared asset from silently ignoring a background state argument. Root then independently ran the tracked browser harness with DHQ_TEST_ASSESSMENTS=1: all9actual modules pass the explicit-value assessment and account-replacement checks, alongside real IndexedDB/retry checks;99fixture reads,0writes,0pageerrors.

On the integrated canonical source:25actualengine groups,7assessment,19capital,5Scoutstate,18Yahootransaction,14protocol/14context/21validation,32session and2actualissuer groups pass. The final actual7-module engine Chrome run uses real IndexedDB and a real second tab: explicit background load preserves active state, foreground one-brain behavior, reload cache adoption, A-to-B cancellation and retry all pass;99fixture reads,0external writes,0pageerrors. Evidence: evidence/integrated-engine-scout/. These remain module/fixture checks rather than full product/live-provider journeys.

Scout current client4c6d709 plus root review075bacf and test-only7a4cc9d passes149unit checks using explicit sourcece24aeb; the nine historic expectations are reconciled with the approved2026free-season boundary without production policy changes. Root corrected AI test ownership and established routing expectations; independent review cleared the test-only delta. After explicit sync to this union, all14Scout npm-test scripts and Vite build pass under Node20.20.2, plus11actualYahoo caller/3Chrome layouts. This is local source/fixture evidence, not publication. Full consumer sync/build/rerun against the final canonical union, exact consumer pin/manifest regeneration, actual-owner merge authority and served-source verification remain required. MFL3a18406+e290fd7 and its account-safe app/lineup/draft callers remain isolated until independently cleared.
