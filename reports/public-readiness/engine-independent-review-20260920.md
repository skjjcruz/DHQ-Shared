# Independent engine review — 1e7ef86

Reviewer lane: native_packaging, which did not author the engine change. Review checkout: `dhq-shared-review-engine-context`, branch `codex/review-engine-context-20260920`, exact candidate 1e7ef860aa18a25f03a46adb8e8859b7cdae9955, base f72c8e4. Only this report and independent evidence are added. No production edits, deployment, credentials or pin changes.

**Current disposition: corrected batch1e7ef86+6d3d604 independently cleared; see the dated follow-up below.** Initial review held1e7ef86 for these four reproducible findings. Their original evidence is retained; the author repaired all four and the reviewer reran the exact reproductions and24groups.

## Material findings

1. **High — conflicting modern identity can adopt another account's private value cache.** `_dhqEngineAccount` (dhq-engine.js:52) takes the cache owner from `session.user.id` after checking only that token/user are nonempty strings. A well-formed JWT payload with owner A, cached user B and a saved B-owned value cache makes `loadLICache()` return true and `App.LI_LOADED` true. This is a client consistency failure, not a claim of bypassing server JWT authentication. Account-owned local data must not be adopted under contradictory token and cached-user identities; malformed modern presence must fail closed without legacy substitution.

2. **High — explicit reload ignores in-place player changes.** The scope tracks player/depth object references, while ready reuse compares only `sourceKey` (dhq-engine.js:766), which excludes player/depth content. After a normal build, changing player100's age from24 to42 in the same players map then calling `loadLeagueIntel()` returns8539; a fresh build for the exact42-year-old inputs returns3759. This also leaves an in-progress build vulnerable to accepting an outdated snapshot after an in-place input mutation. Compare relevant current fingerprints at reload/cache/publication/return boundaries; repeated full-catalog hashing on every scoring getter is unnecessary.

3. **Medium — deduplication bypasses a second caller's cancellation.** At dhq-engine.js:769, identical explicit-state calls return the first caller's promise without checking the second scope. A held caller with `isCurrent:()=>true`, followed by a second identical state with `isCurrent:()=>false`, returns the exact same promise and resolves league111 successfully to the already canceled second caller. Each caller must retain its own lifecycle and cannot borrow the first caller's validity. The existing duplicate-call test only covers equal live no-argument callers.

4. **High — Scout's established age fallback changed healthy valuation behavior.** State-local helpers at dhq-engine.js:782 correctly stop using a later global state, but replace Scout's actual age policy with `p.age || ''`. Exact owning Scout8d6cf54 `js/app.js` derives age from `birth_date`, then uses22 for a rookie when no age exists. With age=null/birth1984, baseline engine plus the actual Scout helper computes age42/value3759; the candidate computes age26/value8861. Existing healthy equality tests omitted the owning Scout helper and therefore did not cover this behavior. Preserve captured state-local caller semantics, including the relevant fingerprint inputs, without reintroducing a mutable-global read after an await. Public/native's simpler age helper has a different established missing-age policy; avoid silently changing that behavior in the correction.

## Independent verification

- `node tests/engine-context-recovery.cjs`: all20 existing actual-module groups pass, including healthy ordinary/superflex/custom-IDP equality,27 pick function comparisons, actual ledger/one-brain equality, cache reload, explicit-state isolation, held account changes, optional storage failure, bounds, provider-chain cancellation and reset/annotation compatibility.
- `NODE_PATH=/Users/jacobc/Projects/warroom-readiness-public-auth/node_modules node tests/engine-context-browser.cjs /tmp/readiness-engine-independent-browser`: actual Chrome390×844 passes real IndexedDB reload and a real second-tab account replacement with held fixture stats. No page errors or external writes. This is the supplied module shell, not a public Empire/Scout product journey.
- Independent actual-module reproductions: `node reports/public-readiness/evidence/engine-independent-review/reproduce.cjs` establishes findings1–3; `node reports/public-readiness/evidence/engine-independent-review/scout-age-regression.cjs` executes exact owning Scout helper and both actual engine revisions for finding4. These scripts assert that the reviewed candidate has the defects; correction tests should invert those expectations. No actual private provider data is involved.
- Source inspection of bound provider request chains, optional cache writes, ledger cancellation/removal guards and one-brain publication found no further material delta issue beyond those four. The report's explicitly open transaction outage/format/pick-evidence limitations remain open, rather than being treated as cleared by this review.

Evidence and rerunnable reproduction scripts: `evidence/engine-independent-review/`. The age reproduction currently refers to the local exact Scout checkout solely to read `git show 8d6cf54:js/app.js`; it does not edit or execute the full app. Review the author's frozen correction, re-run these cases as fixed-behavior regressions, then reassess the bounded disposition. No suite-wide launch signoff is given.

## Follow-up 6d3d604 — bounded clearance

Independently inspected the frozen correction to all four findings and reran all24 actual-module groups. Independently inverted the original reproductions against that exact source: warm in-place age change equals the fresh42-year control; initially and subsequently canceled coalesced callers reject without canceling the live first caller; contradictory modern identity is rejected before any cache read; actual Scout birth-date fallback now matches the full serialized baseline. `verify-corrections.cjs` and `corrections.log` record those checks.

The correction snapshots foreground helper outputs before awaits, includes birth-date/resolved values in the input signature, and compares full value inputs at reload/cache/commit/return boundaries. It keeps hot scoring getters lightweight. Explicit background input uses its own data; it does not invoke another league's mutable global callbacks. Modern identity now requires matching cached user, JWT sub/user_id and future expiry. Each coalesced explicit caller retains its own validity and bounded wait.

**Disposition for 1e7ef86 +6d3d604: no unresolved material finding in this bounded engine context/compatibility delta.** The original four findings above are resolved. Original provider-history outage, format/pick evidence, caller integration and actual product/deployment gates remain open as stated in the author's checkpoint. The prior independent390px browser run covered1e7ef86; this follow-up review independently reran source/module corrections, while the author records the follow-up Chrome rerun. This is not whole-product readiness.
